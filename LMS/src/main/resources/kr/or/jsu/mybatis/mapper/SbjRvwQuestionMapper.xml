<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.jsu.mybatis.mapper.SbjRvwQuestionMapper">

 <!-- 질문 등록 -->
  <insert id="insertSbjRvwQuestion" parameterType="kr.or.jsu.vo.SbjRvwQuestionVO">
    INSERT INTO SBJ_RVW_QUESTION (
      QUESTION_NO,
      SUBJECT_CD,
      QUESTION,
      ANSWER_TYPE_CD
    ) VALUES (
      #{questionNo},
      #{subjectCd},
      #{question},
      #{answerTypeCd}
    )
  </insert>

  <!-- 전체 질문 목록 조회 -->
  <select id="selectSbjRvwQuestionList" resultType="kr.or.jsu.vo.SbjRvwQuestionVO">
    SELECT
      QUESTION_NO,
      SUBJECT_CD,
      QUESTION,
      ANSWER_TYPE_CD
    FROM SBJ_RVW_QUESTION
    ORDER BY QUESTION_NO
  </select>

  <!-- 특정 질문 조회 -->
  <select id="selectSbjRvwQuestion" parameterType="int" resultType="kr.or.jsu.vo.SbjRvwQuestionVO">
    SELECT
      QUESTION_NO,
      SUBJECT_CD,
      QUESTION,
      ANSWER_TYPE_CD
    FROM SBJ_RVW_QUESTION
    WHERE QUESTION_NO = #{questionNo}
  </select>

  <!-- 질문 수정 -->
  <update id="updateSbjRvwQuestion" parameterType="kr.or.jsu.vo.SbjRvwQuestionVO">
    UPDATE SBJ_RVW_QUESTION
    SET
      SUBJECT_CD = #{subjectCd},
      QUESTION = #{question},
      ANSWER_TYPE_CD = #{answerTypeCd}
    WHERE QUESTION_NO = #{questionNo}
  </update>

  <!-- 질문 삭제 -->
  <delete id="deleteSbjRvwQuestion" parameterType="int">
    DELETE FROM SBJ_RVW_QUESTION
    WHERE QUESTION_NO = #{questionNo}
  </delete>
</mapper>