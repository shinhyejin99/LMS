<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.jsu.mybatis.mapper.PrfDegreeMapper">


 <!-- 교수 학위 등록 -->
    <insert id="insertPrfDegree" parameterType="kr.or.jsu.vo.PrfDegreeVO">
        INSERT INTO PRF_DEGREE (
            DEGREE_REGI_ID,
            PROFESSOR_NO,
            DEGREE_NAME,
            INSTITUTION,
            ACQUIRE_AT,
            FINAL_YN
        ) VALUES (
            #{degreeRegiId},
            #{professorNo},
            #{degreeName},
            #{institution},
            #{acquireAt},
            #{finalYn}
        )
    </insert>

    <!-- 교수 학위 전체 목록 조회 -->
    <select id="selectPrfDegreeList" resultType="kr.or.jsu.vo.PrfDegreeVO">
        SELECT
            DEGREE_REGI_ID,
            PROFESSOR_NO,
            DEGREE_NAME,
            INSTITUTION,
            ACQUIRE_AT,
            FINAL_YN
        FROM PRF_DEGREE
    </select>

    <!-- 특정 교수 학위 정보 조회  -->
    <select id="selectPrfDegree" parameterType="string" resultType="kr.or.jsu.vo.PrfDegreeVO">
        SELECT
            DEGREE_REGI_ID,
            PROFESSOR_NO,
            DEGREE_NAME,
            INSTITUTION,
            ACQUIRE_AT,
            FINAL_YN
        FROM PRF_DEGREE
        WHERE DEGREE_REGI_ID = #{degreeRegiId}
    </select>

    <!-- 교수 학위 정보 수정 -->
    <update id="updatePrfDegree" parameterType="kr.or.jsu.vo.PrfDegreeVO">
        UPDATE PRF_DEGREE
        SET
            PROFESSOR_NO = #{professorNo},
            DEGREE_NAME = #{degreeName},
            INSTITUTION = #{institution},
            ACQUIRE_AT = #{acquireAt},
            FINAL_YN = #{finalYn}
        WHERE
            DEGREE_REGI_ID = #{degreeRegiId}
    </update>

    <!-- 교수 학위 정보 삭제 -->
    <delete id="deletePrfDegree" parameterType="string">
        DELETE FROM PRF_DEGREE
        WHERE DEGREE_REGI_ID = #{degreeRegiId}
    </delete>





</mapper>